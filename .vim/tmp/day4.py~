from hashlib import md5
import re

if __name__=="__main__":
    puzzle_input = raw_input("key: ")
    i = 1
    while True:
        m = md5(puzzle_input + str(i))
        if re.match("^000000", m.hexdigest()):
            break
        i += 1
    print i
